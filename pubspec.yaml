name: placement
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0


dependencies:
  flutter:
    sdk: flutter

  # Official packages
  cupertino_icons:
  http: ^1.5.0
  get:
  pinput:
  lottie:
  url_launcher:
  flutter_screenutil:
  flutter_svg: ^2.2.0
  syncfusion_flutter_gauges: ^25.2.6

  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  file_picker: ^10.3.3
  pdf: ^3.11.3
  printing: ^5.14.2
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  shared_preferences: ^2.5.3
  fluttertoast: ^8.2.4
  firebase_core:
  firebase_messaging:
  flutter_local_notifications:
  google_sign_in:
  firebase_auth:
  flutter_facebook_auth:
  image_picker: ^1.1.2
  dio: ^5.7.0
  cached_network_image: ^3.4.1
  animated_text_kit: ^4.2.3
  change_app_package_name: ^1.5.0


flutter:
  uses-material-design: true

  assets:
    - assets/svg/
#    - assets/svg/category/

    - assets/png/
    # - assets/icon/   # agar abhi nahi chahiye to comment rehne do
    - assets/animation/Handshake Loop.json
    - assets/animation/Loading animation.json
    - assets/animation/Placement Assistance.json
    - assets/animation/Welcome Animation.json
